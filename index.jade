doctype html
html
	head
		title МАНЕЖ ТАБЛО
		meta(charset="utf-8")
		link(rel='stylesheet', type='text/css', href='styl/main.css')
		script(type='text/javascript', src='js/jquery-2.1.4.min.js')
		script(type='text/javascript', src='js/main.js')
	body
		.column.one
			each item, i in new Array(30)
				.item(class=(i == 0) ? 'first' : '')
		.column.two
			each item, i in new Array(30)
				.item(class=(i == 0) ? 'first' : '')
		.column.three
			each item, i in new Array(30)
				.item(class=(i == 0) ? 'first' : '')
		.column.four
			each item, i in new Array(30)
				.item(class=(i == 0) ? 'first' : '')
		.column.five
			each item, i in new Array(30)
				.item(class=(i == 0) ? 'first' : '')